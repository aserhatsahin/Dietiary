spring:
  config:
    activate:
      on-profile: dev

  # PostgreSQL - Development
  datasource:
    url: jdbc:postgresql://localhost:5432/dietiary_dev
    username: dietiary
    password: dietiary123!
    driver-class-name: org.postgresql.Driver
  
  # JPA - Development
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Logging - Development
logging:
  level:
    com.dietary: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

# Server - Development
server:
  port: 8081
